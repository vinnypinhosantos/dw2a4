//import { prisma } from "../../prisma";
//import { NoteCreateData, NotesRepository } from "../notes-repository";

//export class PrismaNotesRepository implements NotesRepository {
//    async create({title, texto}: NoteCreateData) {
//        await prisma
//    }
//}